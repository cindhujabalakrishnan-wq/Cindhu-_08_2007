{
  "name": "insurance-platform",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "install": "npm --prefix frontend install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}